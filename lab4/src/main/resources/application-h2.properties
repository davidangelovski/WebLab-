# H2 database configuration
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Enable H2 console
spring.h2.console.enabled=true
spring.h2.console.path=/h2

# Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

#EXAMPLE DATABSE INSERTION
#INSERT INTO location (id, name, address, capacity, description) VALUES (1, 'Venue A', 'Address A', '500', 'Description A');
#INSERT INTO event (id, name, description, popularity_score, location_id) VALUES (1, 'Event A', 'Description A', 4.55, 1);